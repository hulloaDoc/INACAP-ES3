{
  "name": "task-tracker-mock-server",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "start": "json-server --watch db.json --port 3001"
  },
  "dependencies": {
    "json-server": "^0.17.4"
  }
}
